apiVersion: v1
kind: Pod
metadata:
  name: hello-pod
spec:
  containers:
    - name: hello
      image: busybox:1.36
      command: ["sh", "-c"]
      args:
        - |
          echo "hello from a pod running on kind"
          sleep 3600
